# docker compose と go run の両方が読む。コピーして .env にする
# PORT: api が listen するポート / API_PORT: docker compose でホスト側に公開するポート
PORT=8088
API_PORT=8088
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=shiori
DB_PASSWORD=shiori
DB_NAME=shiori
DB_ROOT_PASSWORD=root
CORS_ORIGIN=*
